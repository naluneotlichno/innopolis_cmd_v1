run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - gofmt
    - goimports
    - govet
    - staticcheck
    - typecheck
    - errcheck
    - bodyclose
    - noctx
    - unparam
    - wastedassign
    - misspell
    - gosec
    - mnd
    - gocyclo
    - copyloopvar
    - durationcheck

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
